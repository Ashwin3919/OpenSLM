project:
  name: "<<MODEL_NAME>>"
  seed: 42
  output_dir: "outputs/<<MODEL_NAME>>/"

logging:
  level: "INFO"
  file: null

device:
  type: "auto"   # auto / cuda / mps / cpu
  dtype: "auto"  # auto / float32 / float16 / bfloat16
