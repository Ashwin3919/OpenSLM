# Experiment 001 — baseline
# Standard 30M GPT trained on the same budget and hyperparameters as all other models.
# Run: make train MODEL=miniGPT_config

_includes_:
  - "../base.yaml"
  - "../data/tinystories.yaml"
  - "../model/gpt_small.yaml"
  - "../training/default.yaml"

# No overrides — all defaults from the included files apply.
