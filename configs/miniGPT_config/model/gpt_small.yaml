# 6-layer / 6-head / 384-dim GPT — ~30 M parameters
model_type: gpt
model:
  vocab_size: 50257   # GPT-2 tokenizer vocabulary
  block_size: 128     # context window (tokens)
  n_layer: 6
  n_head: 6
  n_embd: 384
  dropout: 0.0
  bias: true
