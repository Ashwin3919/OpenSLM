# Minimal model for CI, debugging, and architecture prototyping.
# ~3.3M parameters — trains in seconds on CPU.
model_type: gpt
model:
  vocab_size: 50257
  block_size: 16
  n_layer: 2
  n_head: 2
  n_embd: 64
  dropout: 0.0
  bias: true
