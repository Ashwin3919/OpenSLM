project:
  name: "slm"
  seed: 42
  output_dir: "outputs/miniGPT/"

logging:
  level: "INFO"
  file: null

device:
  type: "auto"   # auto / cuda / mps / cpu
  dtype: "auto"  # auto / float32 / float16 / bfloat16
