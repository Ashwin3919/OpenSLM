# Medium model — 12-layer / 12-head / 768-dim (~125M parameters).
# Comparable to GPT-2 small.  Requires ~20GB GPU memory for training.
model:
  vocab_size: 50257
  block_size: 256
  n_layer: 12
  n_head: 12
  n_embd: 768
  dropout: 0.1
  bias: true
